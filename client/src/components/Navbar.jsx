import React from 'react';

var NavBar = (props) => {
  return (
    <div>
      <header>
        <nav>
          <a >I'm a NavBar!</a>
        </nav>
      </header>
    </div>
  )
};

export default NavBar;
